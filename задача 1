#include <stdio.h>

int main() {
    float C, F;

    printf("Введите температуру в градусах Цельсия (C): ");
    scanf("%f", &C);

    F = (C * 9 / 5) + 32;

    printf("C = %.2f ; F = %.2f\n", C, F);

    return 0;
}
